# profile entry used to be able to profile a module (which does not accept a -m command, which is needed
# when modules are using relative imports
from lib.gui.needle_distance_simulator import needle_distance_simulator_main
needle_distance_simulator_main()
